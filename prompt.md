Ти — системний архітектор та інженер багатoагентних AI-систем.  
Твоє завдання — спроєктувати повноцінну систему SEO-агентів, яка складається з кількох спеціалізованих агентів, та видати:

1) Архітектуру системи.  
2) Опис веб-інтерфейсу.  
3) Структуру бази даних (з прикладами таблиць і полів).  
4) YAML-конфігурацію для КОЖНОГО агента окремо (окремі YAML-блоки).

СИСТЕМА ПОВИННА ПРАЦЮВАТИ ЯК SEO-КОМАНДА, ЯКА СКЛАДАЄТЬСЯ З:

1. SEO Team Lead (тимлід SEO)  
2. Project Manager (проджект)  
3. Linkbuilder (лінкбілдер)  
4. Meta Tag Writer (спеціаліст з мета-тегів)  
5. Content Writer (копірайтер, який пише SEO-тексти)  
6. Technical SEO Auditor (технічний SEO-аудитор)

=====================================================================
ЗАГАЛЬНІ ВИМОГИ
=====================================================================

– Мова відповіді: українська.  
– Не використовуй жодних плейсхолдерів на кшталт {{project_name}}, <SITE_NAME>, [[keyword]] тощо.  
  Використовуй реальні та конкретні назви:  
  – назви таблиць: seo_projects, seo_pages, seo_tasks, seo_links, seo_agent_runs, seo_agent_memories;  
  – id агентів: seo_team_lead, seo_project_manager, seo_linkbuilder, seo_meta_writer, seo_content_writer, seo_tech_auditor.  

– Вважай, що система може бути реалізована на такому стеку (приклад, ти можеш його уточнити в тексті):  
  – Back-end: Node.js + TypeScript (Express або Fastify).  
  – Front-end: React / Next.js + Tailwind CSS.  
  – База даних: PostgreSQL.  
  – Оркестрація агентів: окремий бекенд-сервіс, який читає YAML-конфіги агентів.

– Система повинна підтримувати “самонавчання”: зберігати історію запусків агентів, результати, оцінки якості та зафіксовані “кращі практики”.

=====================================================================
ЧАСТИНА 1. АРХІТЕКТУРА СИСТЕМИ
=====================================================================

Опиши архітектуру системи у вигляді:

1. Текстового опису компонентів:
   – Front-end (веб-інтерфейс для користувача і для SEO-команди).  
   – Back-end API (endpoints, які викликають агентів, пишуть/читають з БД, зчитують YAML-конфіги).  
   – Модуль оркестрації агентів (як завдання проходить через різні агенти).  
   – База даних (де що зберігається).  
   – Логи та модуль “самонавчання” (як система вдосконалюється).

2. Послідовність роботи системи на прикладі однієї SEO-задачі (наприклад: оптимізація нової категорійної сторінки):
   – що робить користувач у фронтенді;  
   – як створюється запис у БД;  
   – які агенти викликаються і в якому порядку;  
   – як результат повертається користувачу;  
   – що саме записується в “пам’ять” для самонавчання.

3. Дай просту текстову “архітектурну схему” у вигляді списку або ASCII-схеми:
   Наприклад:  
   Користувач → Web UI → API → Оркестратор → [seo_team_lead → seo_project_manager → інші агенти] → БД → Web UI.

=====================================================================
ЧАСТИНА 2. ВЕБ-ІНТЕРФЕЙС (UI/UX)
=====================================================================

Спроєктуй веб-інтерфейс, який дозволяє:

1. Створювати та переглядати SEO-проєкти (seo_projects).  
2. Додавати сторінки/URL (seo_pages) з полями:  
   – url,  
   – тип сторінки (категорія, картка товару, блог-стаття, лендинг тощо),  
   – основний ключ,  
   – поточний статус оптимізації.

3. Створювати SEO-задачі (seo_tasks), які потім відпрацьовуються агентами:
   – наприклад: “зробити технічний аудит сторінки”, “згенерувати мета-теги”, “написати текст”, “підготувати лінкбілдинг-план”.

4. Переглядати результати роботи кожного агента:
   – для seo_team_lead: стратегія, пріоритети, KPI;  
   – для seo_project_manager: план задач, дедлайни, статуси;  
   – для seo_linkbuilder: список донорів, анкор-лист, пріоритетні сторінки;  
   – для seo_meta_writer: готові title, meta description, H1;  
   – для seo_content_writer: структура та текст;  
   – для seo_tech_auditor: список технічних проблем і рекомендовані фікси.

Опиши:

– Які основні сторінки будуть у веб-інтерфейсі (наприклад: “Проєкти”, “Сторінки”, “Задачі”, “Логи агентів”, “Налаштування агентів”).  
– Які основні компоненти потрібні (форми, таблиці, фільтри, детальні картки).  
– Як приблизно виглядає потік дій користувача (крок за кроком) при роботі з системою.

=====================================================================
ЧАСТИНА 3. БАЗА ДАНИХ (PostgreSQL)
=====================================================================

Спроєктуй базу даних.  
Опиши таблиці у вигляді:

– Назва таблиці.  
– Для кожної таблиці: перелік полів з типами (приблизними) та поясненнями.  
– Для ключових таблиць додай приклад SQL `CREATE TABLE` (без плейсхолдерів).

Обов’язково опиши та/або покажи приклад CREATE TABLE для таких таблиць:

1. seo_projects  
   – id, name, domain, description, created_at, updated_at.

2. seo_pages  
   – id, project_id, url, page_type, main_keyword, language, status, created_at, updated_at.

3. seo_tasks  
   – id, project_id, page_id, task_type, status, assigned_agent_id, input_data, output_data, created_at, updated_at.

4. seo_agent_runs  
   – id, agent_id, task_id, started_at, finished_at, status, quality_score, notes.

5. seo_agent_memories  
   – id, agent_id, memory_type, content, created_at.

6. seo_links (для лінкбілдінгу)  
   – id, project_id, source_url, target_page_id, anchor, link_type, status, created_at.

Можеш додати ще таблиці, якщо вважаєш їх потрібними (наприклад, seo_checklists).

=====================================================================
ЧАСТИНА 4. YAML-ФАЙЛИ ДЛЯ КОЖНОГО АГЕНТА
=====================================================================

Створи окремі YAML-конфіги для кожного агента.  
Для КОЖНОГО з 6 агентів згенеруй ПОВНИЙ YAML-блок, який можна зберегти у файл.  
Не використовуй плейсхолдери — пиши конкретні значення.

Структура YAML (приклад, який треба конкретизувати та заповнити):

– Для кожного агента створи YAML з такими ключами (ти можеш додати ще свої, але ці мають бути):

agent_id: (наприклад, seo_team_lead)  
name: (наприклад, "SEO Team Lead")  
description: (чіткий опис ролі агента)  
goals:  
  - (конкретні цілі агента)  
  - ...  
inputs:  
  - опис того, які дані агент отримує (запит користувача, дані сторінки, результати інших агентів)  
outputs:  
  - опис того, що агент повертає (стратегія, план, мета-теги, фікси тощо)  
tools:  
  - опис логічних “інструментів”, які агент може використовувати (аналітика, краулінг, GA4, GSC, генерація тексту)  
memory:  
  enabled: true  
  memory_table: seo_agent_memories  
  strategy: >  
    Коротко опиши, як агент повинен використовувати пам’ять (шукати попередні рішення, шаблони, кращі практики).  
workflow:  
  receives_from:  
    - перелік agent_id, від яких цей агент логічно отримує дані (якщо є)  
  sends_to:  
    - перелік agent_id, яким цей агент передає результат (якщо є)  
language: "uk"

Зроби окремий YAML-блок для кожного агента:

1) seo_team_lead  
2) seo_project_manager  
3) seo_linkbuilder  
4) seo_meta_writer  
5) seo_content_writer  
6) seo_tech_auditor  

Кожен YAML-блок виведи у своєму окремому код-блоці:

# seo_team_lead.yaml
...
# seo_project_manager.yaml
...
=====================================================================
ЧАСТИНА 5. САМОНАВЧАННЯ СИСТЕМИ

Окремим розділом опиши:

Як система буде “самонавчатися” на базі таблиць seo_agent_runs та seo_agent_memories.

Які саме дані варто зберігати після кожного запуску агентів (наприклад: які шаблони мета-тегів спрацювали краще, які технічні фікси дали результат, які типи донорів найефективніші).

Як у майбутньому можна буде використовувати ці дані, щоб:
– оновлювати промпти агентів;
– змінювати параметри YAML-конфігів;
– покращувати пріоритезацію задач.

=====================================================================
ФОРМАТ ФІНАЛЬНОЇ ВІДПОВІДІ

Виведи результат структуровано:

"I. Архітектура системи"

"II. Веб-інтерфейс"

"III. База даних (PostgreSQL)"

"IV. YAML-конфіги агентів" (6 окремих YAML-блоків)

"V. Самонавчання системи"

Пам’ятай:
– жодних плейсхолдерів;
– усі приклади — конкретні;
– відповіді — українською.
